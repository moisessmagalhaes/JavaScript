// const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovada';

const resultado = nota => {
   return nota >= 7 ? 'Aprovado' : 'Reprovada';
} 

console.log(resultado(7.1));
console.log(resultado(6.8))